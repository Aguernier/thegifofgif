{% extends 'base.html.twig' %}

{% block title %}home !{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            {% for post in posts %}
                <div class="col-md-2">
                    {{ include("_includes/post-card.html.twig") }}
                </div>
            {% endfor %}
        </div>
        
        <div class="pagination">
            {{ knp_pagination_render(posts) }}
        </div>
    </div>
{% endblock %}
